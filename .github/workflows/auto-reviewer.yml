name: Auto Reviewer by Diff Keyword
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  check-diff:
    runs-on: ubuntu-latest
    # This line tells the runner to use Node 24 for all actions
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Scan Diff for Keyword
        id: scan
        run: |
          git fetch origin ${{ github.base_ref }}
          if git diff origin/${{ github.base_ref }}...HEAD | grep "^\+" | grep -i "THATSCHEMA"; then
            echo "match=true" >> $GITHUB_OUTPUT
          else
            echo "match=false" >> $GITHUB_OUTPUT
          fi

      - name: Assign Team
        if: steps.scan.outputs.match == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.pulls.requestReviewers({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              reviewers: ['pangan'],
              team_reviewers: ['your-team-slug'] 
            })